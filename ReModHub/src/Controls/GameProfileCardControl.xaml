<UserControl x:Class="ReModHub.Controls.GameProfileCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ReModHub.Controls"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="240">
    <Border Width="240"
            Margin="6"
            Padding="10"
            CornerRadius="8"
            Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
        <Border.InputBindings>
            <MouseBinding Gesture="LeftDoubleClick"
                          Command="{Binding LaunchCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileCardControl}}"
                          CommandParameter="{Binding Profile, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileCardControl}}" />
        </Border.InputBindings>
        <Grid DataContext="{Binding Profile, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileCardControl}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding DisplayName}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                       TextTrimming="CharacterEllipsis"/>
            <Button Grid.Column="1"
                    Padding="4,0"
                    Margin="6,-2,0,0"
                    VerticalAlignment="Top"
                    FontSize="16"
                    Content="..."
                    Tag="{Binding RelativeSource={RelativeSource AncestorType=local:GameProfileCardControl}}"
                    Click="OnMenuClicked">
                <Button.ContextMenu>
                    <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="編集"
                                  Command="{Binding EditCommand}"
                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="削除"
                                  Command="{Binding DeleteCommand}"
                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>

            <TextBlock Grid.Row="1"
                       Grid.ColumnSpan="2"
                       Text="{Binding VersionName, StringFormat=version: {0}}"
                       Margin="0,4,0,0"
                       Opacity="0.75"/>
        </Grid>
    </Border>
</UserControl>
