<UserControl x:Class="ReModHub.Controls.ModManifestCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ReModHub.Controls"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="240">
    <Border Width="240"
            Margin="6"
            Padding="10"
            CornerRadius="8"
            Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
        <StackPanel DataContext="{Binding Manifest, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:ModManifestCardControl}}">
            <TextBlock Text="{Binding DisplayName}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="{Binding VersionName, StringFormat=version: {0}}"
                       Margin="0,4,0,0"
                       Opacity="0.75"/>
        </StackPanel>
    </Border>
</UserControl>
