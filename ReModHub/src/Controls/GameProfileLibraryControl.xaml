<UserControl x:Class="ReModHub.Controls.GameProfileLibraryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ReModHub.Controls"
             mc:Ignorable="d"
             d:DesignHeight="350"
             d:DesignWidth="1000">
    <Grid Background="{ui:ThemeResource SolidBackgroundFillColorBaseBrush}">
        <ScrollViewer Margin="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="240"
                                Margin="6"
                                Padding="10"
                                CornerRadius="8"
                                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                              Command="{Binding LaunchCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}"
                                              CommandParameter="{Binding}" />
                            </Border.InputBindings>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding DisplayName}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <Button Grid.Column="1"
                                        Padding="4,0"
                                        Margin="6,-2,0,0"
                                        VerticalAlignment="Top"
                                        FontSize="16"
                                        Content="..."
                                        Tag="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}"
                                        Click="OnMenuClicked">
                                    <Button.ContextMenu>
                                        <ContextMenu DataContext="{Binding PlacementTarget, RelativeSource={RelativeSource Self}}">
                                            <MenuItem Header="編集"
                                                      Command="{Binding Tag.EditCommand}"
                                                      CommandParameter="{Binding DataContext}"/>
                                            <MenuItem Header="削除"
                                                      Command="{Binding Tag.DeleteCommand}"
                                                      CommandParameter="{Binding DataContext}"/>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>

                                <TextBlock Grid.Row="1"
                                           Grid.ColumnSpan="2"
                                           Text="{Binding VersionName}"
                                           Margin="0,4,0,0"
                                           Opacity="0.75"/>
                                <TextBlock Grid.Row="2"
                                           Grid.ColumnSpan="2"
                                           Text="{Binding BaseGameReference.Uuid, StringFormat=Base: {0}}"
                                           Margin="0,6,0,0"
                                           FontSize="12"
                                           Opacity="0.7"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
