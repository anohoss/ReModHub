<UserControl x:Class="ReModHub.Controls.GameProfileLibraryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ReModHub.Controls"
             mc:Ignorable="d"
             d:DesignHeight="350"
             d:DesignWidth="1000">
    <Grid Background="{ui:ThemeResource SolidBackgroundFillColorBaseBrush}">
        <ScrollViewer Margin="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:GameProfileCardControl Profile="{Binding}"
                                                      LaunchCommand="{Binding LaunchCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}"
                                                      EditCommand="{Binding EditCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}"
                                                      DeleteCommand="{Binding DeleteCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:GameProfileLibraryControl}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
